#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    
    int res1=max(a,max(b,c));
    int res2=min(a,min(b,c));
    int ans=res1-res2;
    
    cout<<ans<<'\n';
    return 0;
}
